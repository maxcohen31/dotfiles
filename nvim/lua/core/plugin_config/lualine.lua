-- gruvbox
--require('lualine').setup {
 --options = {
 --   icons_enabled = true,
-- theme = 'gruvbox',
--  },
  --sections = {
    --lualine_a = {
      --{
        --'filename',
       -- path = 1,
      --}
    --}
  --}   
-- }

require('lualine').setup {
    options = {
        theme = "catppuccin-mocha"
      -- ... the rest of your lualine config
  }
}
